class ConvocatoriaRepositoryPort {
  getAllConvocatorias() { throw new Error('No implementado'); }
  getConvocatoriaById(id) { throw new Error('No implementado'); }
  crearConvocatoria(data) { throw new Error('No implementado'); }
  actualizarConvocatoria(id, data) { throw new Error('No implementado'); }
  eliminarConvocatoria(id) { throw new Error('No implementado'); }
}

module.exports = ConvocatoriaRepositoryPort;
